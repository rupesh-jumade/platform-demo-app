services:

  app:
    build: .
    container_name: platform-demo-app

    ports:
      - "5000:5000"

    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    container_name: redis-server