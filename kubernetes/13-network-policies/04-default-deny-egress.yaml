apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-egress
  namespace: network-policy-lab
spec:
  podSelector:
    matchLabels:
      app: client
  policyTypes:
    - Egress
